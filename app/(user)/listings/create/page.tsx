import { Button } from "@/components/ui/button"
import {
    Form,
    FormControl,
    FormDescription,
    FormField,
    FormItem,
    FormLabel,
    FormMessage
} from "@/components/ui/form"
import { Input } from "@/components/ui/input"

export default function CreateListings() {
    return (
        <main className="bg-muted text-foreground flex justify-center items-center">
        </main>
    )
}